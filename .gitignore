.venv/
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
build/
dist/

.env

# Downloaded filing HTML + cached EDGAR metadata — re-fetchable, don't commit.
data/raw/
data/cache/
data/retrieval_a7.json

# Working copy of the build plan. Canonical original lives in
# ~/.claude/plans/want-to-design-a-golden-barto.md
docs/PLAN.md
docs/*.bak

# Phase 2 plan doc — working notes, not committed. README covers the project.
docs/phase-2-plan.md

# Claude Code skills — a personal tool install (langsmith-skills etc.), not
# project source. Re-fetch from the upstream repo rather than committing it.
.claude/
